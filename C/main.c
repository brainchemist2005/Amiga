#include<stdio.h>
int main(){
    int x,y,result;

    printf("enter a number for x and y : ");
    scanf("%d %d ",&x, &y);
    result = x + y ;
    printf("the result is %d+%d is %d",x,y,result);
    return 0;
      }